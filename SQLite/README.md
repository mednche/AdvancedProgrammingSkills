# Write and read in an SQLite database

This is a simple project composed of two scripts:

- *createdb.py* creates an SQLite database and write in it.
- *readdb.py* opens the SQLite database and read throw its rows.
